// ici on pourra utiliser les api node
const swipl = require('swipl')

/*
swipl.call("consult('ia/test.pl')")

const ret = swipl.call('ancetre3(X,Y).');

if (ret) {
  console.log(`${ret.X}`)
  console.log(`${ret.Y}`)
  document.writeln(`${ret.X}`)
  document.writeln(`${ret.Y}`)
} else {
  console.log('Call failed.');
}
*/

console.log('Hello you')